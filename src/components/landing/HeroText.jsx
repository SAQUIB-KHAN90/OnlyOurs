const HeroText = () => {
  return (
    <div className="space-y-6 max-w-xl">
      <h1 className="text-5xl md:text-6xl font-heading text-primary leading-tight">
        OnlyOurs
      </h1>

      <h2 className="text-2xl font-heading text-textPrimary">
        What’s ours, stays ours
      </h2>

      <p className="text-lg font-body text-textMuted leading-relaxed">
        A private space for couples to save memories, share feelings,
        listen together, and write the words the world never gets to read.
      </p>
    </div>
  );
};

export default HeroText;
